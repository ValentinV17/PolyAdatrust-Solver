# Core dependency
numpy>=1.20


# Optional: enables JIT compilation of the inner kernels for a speed-up.
# PolyTR runs without it (pure-NumPy fallback), so it is left commented out.
# scipy>=0.16
# numba>=0.56
